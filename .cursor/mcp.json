{
  "mcpServers": {
    "beads": {
      "command": "uvx",
      "args": ["beads-mcp"],
      "env": {
        "BEADS_PATH": "${userHome}/.local/bin/bd",
        "BEADS_WORKING_DIR": "${workspaceFolder}"
      }
    }
  }
}
